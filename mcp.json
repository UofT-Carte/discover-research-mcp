{
  "_comment": "Template. Replace the --directory path with the absolute path to your clone, then copy this entry into your MCP client's config. An MCP client starts the server from its own working directory, so the path cannot be relative.",
  "mcpServers": {
    "discover-research-mcp": {
      "type": "stdio",
      "command": "uv",
      "args": [
        "run",
        "--directory",
        "/absolute/path/to/discover-research-mcp",
        "python",
        "server.py"
      ]
    }
  }
}
