[project]
name = "discover-research-mcp"
version = "0.2.0"
description = "MCP server for the University of Toronto Discover Research portal"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.14.3",
    "fastmcp>=3.4.5,<4",
    "httpx>=0.28.1",
]

[project.scripts]
discover-research-mcp = "discover_research_mcp.server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/discover_research_mcp"]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "pytest-asyncio>=1.3.0",
    "pytest-httpx>=0.36.0",
    "ruff>=0.16.1",
]

[tool.ruff]
# Research notes quote upstream source verbatim at the v3.4.5 tag. Reformatting
# those snippets would misrepresent what the cited source actually says.
extend-exclude = ["docs"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
